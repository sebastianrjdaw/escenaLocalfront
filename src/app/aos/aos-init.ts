// src/app/aos-init.ts

import AOS from 'aos';
import 'aos/dist/aos.css';

AOS.init({
  duration: 800,  // Duración de la animación en milisegundos
  easing: 'ease-in-out',  // Tipo de animación (puedes elegir otros valores)
});
